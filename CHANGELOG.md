# Changelog

## 2026-07-12

### 模型与识别

- 接入单类别 `YOLO26n hard-v2` 批改标记模型，默认权重为 `ocr_service/models/error_mark_yolo26n_hard_v2.pt`。
- 将通用 `error_pipeline` 从旧 OBB 推理迁移为普通 Detect 轴对齐框推理，移除生产代码中的 `result.obb` 依赖；OBB 训练脚本仅保留为历史实验工具。
- 答题卡批改检测采用 SAHI `640x640` 滑窗、20% 重叠、IoU NMS；候选阈值为 `0.03`，输出阈值为 `0.40`。
- 新增双阈值判定：置信度不低于 `0.80` 的标记作为明确错题证据，`0.40-0.80` 的标记只进入人工复核。
- 模型只在答题卡页面运行；题目页不生成错题标记。模型不可用或执行失败时才回退到红色几何规则。
- 在原始图片分辨率执行模型推理，并将检测框缩放回 OCR 坐标系，避免 OCR 缩图造成小红叉丢失。
- 逐张识别请求改为同时传递1600px分析图和原始上传图：OCR继续使用分析图，YOLO每页都使用原图检测，再映射回OCR坐标。
- 新增OCR中间结果调试产物：布局标记图、YOLO原图标记图、OCR坐标合并图和检测元数据JSON，支持环境变量或单次请求开启。
- 修复宽题干横向覆盖导致下一题批改框被上一题抢占的问题；题号绑定改为先分栏、再按题号纵向区间归属。
- 单张模式新增错题候选局部证据裁图并随整页发送给MiniMax；选择题强制区分学生手写选项与印刷选项标签，看不清时禁止猜测。
- 候选局部证据改为从原始上传图按比例裁剪，不再从1600px分析图裁剪，提升红叉覆盖下手写选项的可辨识度。
- OCR 健康检查新增 `gradingDetector` 状态，OCR 请求默认超时调整为 120 秒。

### 训练工具

- 新增 Detect 训练集切片脚本，支持 640x640、20% 重叠、标签裁剪和困难负样本筛选。
- 新增 FP/FN 困难样本挖掘脚本，固定 test 集不参与训练，避免评估数据泄漏。

## 2026-07-11

### 新增

- 新增白皮书方案对应的企业级错题提取模块 `ocr_service/error_pipeline`：
  - YOLO11-OBB + SAHI 滑窗推理及旋转多边形 NMS
  - OCR 四点坐标解析、双栏聚类、主题/小题几何归属和原子切图
  - MiniMax VLM 并发精修、超时重试、JSON提取和字段校验
  - Copy-Paste 离线增强、透明度/缩放/旋转扰动和 Ultralytics OBB 标签输出
  - 完整命令行入口与模块使用文档
- 新增 YOLO11-OBB 训练工具链：91张试卷按试卷组无泄漏划分、红叉伪标签初始化、OBB标签质检、MPS训练、独立测试集评估和 `best.pt` 输出。
- 新增“整卷识别”上传模式，支持题目页和答题卡混合上传，不依赖上传顺序。
- 新增题目页、答题卡自动分类，并按题号将答题卡上的作答和批改证据关联到题目页。
- 支持多张题目页和多张答题卡组合分析；题目页仅作为题干参考，错题标记和学生作答只采信答题卡。
- 引入 OCRAutoScore 的 YOLOv8 答题卡布局模型，识别学生信息、主观题、填空题和客观题区域。
- OCR 响应新增 `layoutRegions` 和 `pageRoleHint`，将答题区域坐标、类别、置信度提供给页面分类和 MiniMax 分析。
- 新增答题区域近重复框过滤，并保留整页 OCR 兜底。
- 新增人工复核“待处理 / 已处理”视图，以及复核进入时间和处理时间展示。
- 新增错题解析内容状态：`解析补全中`、`解析已完整`、`补全失败`。
- 人工确认为错题后，自动检查解析完整度；内容不足时调用 MiniMax 补全：
  - 完整解题过程与深入浅出的讲解
  - 错因分析和通用解题套路
  - 相关知识点、母题、易错点和变式练习
  - 深圳题型风格及带来源的深圳真题参考
- 错题详情页新增“通用解题套路”“相关知识点”“母题与深圳题型”展示。
- 深圳真题必须包含来源名称和 HTTP(S) 链接；来源不完整时自动降级为“AI 模拟题”或“深圳题型风格”。

### 优化

- MiniMax 增加专用答题卡批改检测流程，重点识别红叉、半叉、半勾和扣分数字。
- 对混合试卷按题号逐题调用 MiniMax，明确传递题目页索引和答题卡索引，降低跨页串题和乱答风险。
- 答题卡布局检测改为独立子进程运行，隔离 PyTorch 与 PaddleOCR，避免原生运行时冲突导致 OCR 服务退出。
- OCRAutoScore 模型不可用、超时或推理失败时自动继续整页 OCR，不阻断上传分析。
- 上传结果增加页面角色文案：答题卡显示批改解析，题目页显示“作为参考”。
- 需要人工确认或 AI 证据不足的分析只进入人工复核，不进入错题本、知识漏洞和模拟练习。
- 上传历史状态可根据关联错题的复核结果同步刷新。
- 人工复核结果与 AI 内容补全解耦：补全失败不改变“已确认错题”的人工结论。

### 修复

- 提升单张已批改试卷识别准确率：红色连通块改用双对角线结构判断叉号，不再仅凭宽高将红勾、长斜线和批注误判为错题。
- 单张模式只采用置信度不低于 `0.60` 的批改候选，并禁止 MiniMax 脱离候选整页新增错题。
- 过滤公式中的伪题号 `0` 和章节标题，增强解答题主题号与 `(2)`、`(4)` 等小题号的空间绑定。
- 修复同步单张上传接口遗漏 `paperLayout=independent_pages`，导致旧的候选强制补全规则继续生效的问题。
- MiniMax 输出损坏时只按高置信度候选降级到人工复核，不再恢复低置信度噪声。
- 使用 `2916.JPG` 建立验收样例，候选结果收敛为 `14(2)`、`14(4)`、`15`、`16`。
- 修复题目页中的手写痕迹、勾叉或颜色可能被错误当作老师批改证据的问题。
- 修复答题卡与题目页未正确对应时，AI 仍猜测题目或生成错误解析的问题。
- 修复同一道题重复上传或重跑时反复创建待复核记录的问题。
- 修复 OCR 文字存在空格、标点或轻微识别差异时，已人工处理的题目无法继承复核结果的问题。
- 已确认为错题的记录保持 `confirmed_wrong`，后续 AI 识别不能覆盖人工结论。
- 已确认为非错题的记录保持 `not_wrong`，再次识别不会重新进入人工复核。
- 修复已处理记录缺少处理时间和解析完整度状态的问题。
- 修复安装 Ultralytics 后 ESLint 误扫描 `.venv-ocr` 内第三方 JavaScript 的问题。
- 更新错题列表空状态测试文案，使其与分类后的实际界面一致。

### 数据库

- `AnalysisJob` 新增 `relatedImagesJson`，保存整卷任务关联的题目页、答题卡和页面角色。
- `Mistake` 新增：
  - `contentStatus`：内容补全状态
  - `contentJson`：结构化知识点、套路、母题和例题
  - `contentError`：补全失败原因
  - `contentUpdatedAt`：最近补全时间
- 新增迁移：
  - `20260711090000_composite_paper_answer_sheet`
  - `20260711160000_mistake_content_enrichment`

### 模型与依赖

- 新增 `ocr_service/models/answer_sheet_layout.pt`。
- 新增 `ultralytics>=8.3,<9.0` 依赖及 OCR 布局检测配置项。
- 补充 OCRAutoScore 模型来源、用途和 AGPL-3.0 许可证说明。

### 测试与验证

- 新增整卷页面角色、题号匹配、答题区域去重和人工复核结果继承测试。
- 前端及服务端单元测试：111 项通过。
- OCR Python 单元测试：11 项通过。
- ESLint 检查通过。
- Prisma 数据库迁移和客户端生成通过。
- Next.js 生产构建通过。
- 使用 `2906.JPG`、`2907.JPG`、`2908.JPG`、`2909.JPG` 验证题目页/答题卡分类和布局检测。
- `2908.JPG` 实际 OCR 接口返回 7 个答题区域、81 个文字块和 15 个题号候选。
